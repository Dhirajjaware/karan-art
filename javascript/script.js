'use strict';

const containerImage = document.querySelector('.row');

// containerImage.addEventListener('mouseover', function (e) {
//   const img = e.target.closest('img');

//   if (!img) return;

// });
